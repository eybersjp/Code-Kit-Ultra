{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "execution.failed",
  "type": "object",
  "required": ["eventId", "eventType", "occurredAt", "correlationId", "orgId", "workspaceId", "projectId", "runId", "payload"],
  "properties": {
    "eventId": { "type": "string" },
    "eventType": { "const": "execution.failed" },
    "occurredAt": { "type": "string", "format": "date-time" },
    "correlationId": { "type": "string" },
    "orgId": { "type": "string" },
    "workspaceId": { "type": "string" },
    "projectId": { "type": "string" },
    "runId": { "type": "string" },
    "payload": {
      "type": "object",
      "required": ["stepId", "adapter", "message"],
      "properties": {
        "stepId": { "type": "string" },
        "adapter": { "type": "string" },
        "message": { "type": "string" },
        "suggestedFix": { "type": "string" }
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": true
}
