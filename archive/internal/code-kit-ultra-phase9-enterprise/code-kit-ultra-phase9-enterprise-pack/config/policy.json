{
  "mode": "team-safe",
  "rules": {
    "requireApprovalFor": ["github"],
    "maxRetries": 2,
    "allowRollback": true,
    "blockCommands": ["rm -rf /", "sudo rm -rf /", "mkfs"],
    "requirePrReview": true
  }
}
