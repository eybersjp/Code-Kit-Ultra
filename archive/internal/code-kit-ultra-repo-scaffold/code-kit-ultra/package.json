{
  "name": "code-kit-ultra",
  "version": "0.1.0",
  "private": true,
  "description": "Autonomous AI product-building operating system scaffold",
  "workspaces": [
    "packages/*",
    "adapters/*",
    "apps/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist packages/*/dist adapters/*/dist apps/*/dist",
    "cli": "node dist/apps/cli/src/index.js",
    "check": "tsc -b --pretty false",
    "lint": "echo 'Add your linter here'",
    "test": "echo 'Add your tests here'"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
