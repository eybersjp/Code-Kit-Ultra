# Disaster Recovery

## Recovery examples
- reset `.env` from `.env.example`
- restore `.codekit/` if backed up
- regenerate release artifact with `npm run build:public-release`
- temporarily switch to safer profiles