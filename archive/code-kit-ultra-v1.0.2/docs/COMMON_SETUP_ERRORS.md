# Common Setup Errors

## `.env` missing
Run:
```bash
cp .env.example .env
```

## script permission denied
Run:
```bash
chmod +x scripts/*.sh
```

## artifact folder missing
Create it:
```bash
mkdir -p artifacts/test-runs
```
