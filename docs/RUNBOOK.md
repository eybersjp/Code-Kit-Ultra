# Runbook

## Startup
1. copy `.env.example` to `.env`
2. `npm install`
3. `npm run bootstrap`
4. `npm run preflight`

## Diagnostics
- `npm run validate-env`
- `npm run ck -- adapters`
- `npm run ck -- metrics`