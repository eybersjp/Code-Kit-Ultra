name: Validate Config

on:
  push:
  pull_request:

jobs:
  config:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: test -f .env.example
      - run: test -f config/runtime-hardening.json
      - run: test -f VERSION